# Telus-Health-Markdown-Converter
The Markdown Converter is a 100% client-side web application. It does **not** require a backend server since all processing happens in the browser. However, to share it publicly, you need to host it on a web server that can serve HTML files.
